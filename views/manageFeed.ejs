<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Feed</title>
    <link rel="stylesheet" href="main.css"> 
</head>
<body>
    <header class="header">
        <h1 id="manage-feed-headline">Manage Feed</h1>
        <form class="search-form" action="/search" method="get">
            <input type="text" name="query" placeholder="Search...">
            <button type="submit">Search</button>
        </form>                        
        <form action="/CreateBookNote" method="get">
            <button type="submit" class="create-button"> Create New book note</button>
        </form>
    </header>
    <main class="feed-container">
        <h2 class="username">Welcome, <%= username %>!</h2>
        <div class="cards-container">
            <% posts.forEach(post => { %>
                <div class="card">
                    <div class="card-content">
                        <h3 class="card-title"><%= post.title %></h3>
                        <img src="<%= post.picture %>" alt="<%= post.title %>" class="card-image">
                        <p class="card-rating">Rating: <%= post.rating %></p>
                        <p class="card-date"><%= new Date(post.date).toLocaleDateString() %></p>
                        <p class="card-description"><%= post.description %></p>
                        <form action="/edit-card/<%= post.id %>" method="get">
                            <button type="submit" class="edit-button">Edit</button> 
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
    </main>
</body>
</html>